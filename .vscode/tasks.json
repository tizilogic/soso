{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Debug Build",
            "type": "shell",
            "command": "${workspaceFolder}/make --compile --debug --compiler clang",
            "windows":{
                "command": "${workspaceFolder}/make.bat --compile --debug"
            }
        }
    ]
}